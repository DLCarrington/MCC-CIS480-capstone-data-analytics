This folder contains all Power BI files.

Power BI data updates can be accomplished by modifying the query parameter DataFolderPath_Parameter with the path to the folder containing data files.

The online data source from Google should refresh correctly when connected to the Internet.
